{
  "name": "wx-draft-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "基于 Cloudflare Workers + Hono 的微信公众号草稿推送网关（免服务器 · 免备案）",
  "dependencies": {
    "hono": "^4.13.7"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy dist/pages --project-name=wx-draft-worker",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.107.0"
  }
}